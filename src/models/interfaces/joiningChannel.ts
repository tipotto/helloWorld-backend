export default interface joiningChannel {
  id: string;
  name: string;
  avatarLink: string;
  lastMessage: string;
  unreadCounter: number;
  aboutChannel: string;
  isAdmin: boolean;
  date: any;
}
